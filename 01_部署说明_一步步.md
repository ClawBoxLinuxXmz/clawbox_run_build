# 01 部署说明 —— ClawBox 从零部署

> 适用:空核桃派2B(原厂 Debian 12, 已联网, root 可登录)。
> **日常部署只需一行命令(见下)**;手动分步/常见问题供排查定位。
> 本包已实机验证:2026-08-28 删板重装一次跑通(EXIT=0, 全 6 步)。

---

## 一键部署(设备端, 日常用)

文件传输由**部署者自己完成**(需 IP/密码);传输就绪后,设备端**一条命令**完成全部部署
(外设守护 → Node22+swap → 前端 build → systemd 服务 → OpenClaw 网关+6 插件):

**① 建用户 + 传输(需 IP/密码):**

```bash
# 设备端: 建用户(创建 /home/clawbox 供 scp)
ssh root@<设备IP> "useradd -m -s /bin/bash clawbox; usermod -aG dialout,sudo,audio,video,netdev,i2c clawbox"

# PC 端: 传 3 目录 + 一键脚本(前端源码目录名是中文, 先转 ASCII 名避免乱码)
scp -r clawbox-deploy root@<设备IP>:/home/clawbox/
scp -r frontend-systemd root@<设备IP>:/home/clawbox/clawbox-deploy/
xcopy /E /I 前端源码 %TEMP%\frontend-src
scp -r %TEMP%\frontend-src root@<设备IP>:/tmp/frontend-src
scp deploy_all.sh root@<设备IP>:/home/clawbox/
```

**② 部署(设备端一条命令):**

```bash
bash /home/clawbox/deploy_all.sh
```

- ⚠️ 中途若提示 "SPI 需重启":设备 `reboot` 后**重跑同一命令**(脚本幂等,自动跳过已完成)。
- 中断/失败:修复后重跑即可(幂等)。**已实机验证**(2026-08-28, 225 删板重装一次跑通, EXIT=0)。
- 跑完最后一步(**手动**):浏览器 `http://<设备IP>/` 走 /setup 向导配 AI Key + 聊天渠道。

---

## 传输 + 手动部署命令速查(备用/排查)

> **第 1-2 步 = 传输阶段, 部署者自己完成(需 IP/密码)**;第 3 步起 = deploy_all.sh 内部内容。
> 细节见脚本 `deploy_all.sh`。

```bash
# 1. 建用户(设备端; 创建 /home/clawbox 供 scp 目标)
useradd -m -s /bin/bash clawbox
usermod -aG dialout,sudo,audio,video,netdev,i2c clawbox

# 2. 传文件(PC 端 scp; 前端源码目录名是中文, 先转 ASCII 名避免乱码)
scp -r clawbox-deploy root@<设备IP>:/home/clawbox/
scp -r frontend-systemd root@<设备IP>:/home/clawbox/clawbox-deploy/
scp -r frontend-src root@<设备IP>:/tmp/frontend-src
scp deploy_all.sh root@<设备IP>:/home/clawbox/

# 3. 一键部署(设备端)  — 也可手动分步如下:
bash /home/clawbox/deploy_all.sh

# 4. 外设守护(SPI 首次启用需重启; 看门狗默认关)
cd /home/clawbox/clawbox-deploy/clawbox-peripheral && bash install.sh

# 5. Node22 + swap
wget -O /tmp/n.txz https://nodejs.org/dist/v22.23.2/node-v22.23.2-linux-arm64.tar.xz
tar -xJf /tmp/n.txz -C /opt && mv /opt/node-v22.23.2-linux-arm64 /opt/node22
ln -sf /opt/node22/bin/{node,npm,npx} /usr/local/bin/
fallocate -l 1.5G /swapfile && chmod 600 /swapfile && mkswap /swapfile && swapon /swapfile
echo '/swapfile none swap sw 0 0' >> /etc/fstab

# 6. 前端(前端源码已是标准布局: 配置留根+src/源码; 用 cp -a 勿 mv, 保留 data/)
export PATH=/opt/node22/bin:$PATH
cp -a /tmp/frontend-src/. /home/clawbox/clawbox/
cd /home/clawbox/clawbox && npm install && npm run build
chown -R clawbox:clawbox /home/clawbox/clawbox

# 7. systemd 服务
cd /home/clawbox/clawbox-deploy/frontend-systemd && bash install_services.sh /opt/node22/bin/node
systemctl enable --now clawbox-setup

# 8. OpenClaw 网关(本体+配置+6插件)
npm install -g openclaw@2026.7.1-2 --prefix /home/clawbox/.npm-global
chown -R clawbox:clawbox /home/clawbox/.npm-global
mkdir -p /home/clawbox/.openclaw
if [ ! -f /home/clawbox/.openclaw/openclaw.json ]; then
  TMP_OC=$(mktemp /tmp/clawbox-openclaw.XXXXXX)
  cp /home/clawbox/clawbox-deploy/frontend-systemd/openclaw.json "$TMP_OC"
  TOKEN=$(openssl rand -hex 21)
  sed -i "s/CHANGE_ME_RANDOM_HEX/$TOKEN/" "$TMP_OC"
  install -o clawbox -g clawbox -m 0600 "$TMP_OC" /home/clawbox/.openclaw/openclaw.json
  rm -f "$TMP_OC"
else
  chown clawbox:clawbox /home/clawbox/.openclaw/openclaw.json
  echo "openclaw.json 已存在, 保留现有配置"
fi
# 逐个装 6 插件: @tencent-weixin/openclaw-weixin@2.4.6 / @openclaw/qqbot /
#   clawhub:@openclaw/whatsapp / @openclaw/deepseek-provider / @openclaw/feishu / clawhub:@openclaw/line
#   (telegram 为 openclaw 内置, 无需安装, openclaw.json 已启用)
sudo -u clawbox -H env PATH=/opt/node22/bin:$PATH /home/clawbox/.npm-global/bin/openclaw plugins install <插件名>
systemctl enable --now clawbox-gateway

# 9. 网页配置(手动): 浏览器 /setup 配 AI Key + 渠道扫码
```

> 手动注意:
> - 硬件看门狗**默认不启用**(opt-in);探针契约未验证前勿开(会误重启整板)。
> - 前端源码目录名是中文,cmd 直传可能乱码 → 先转 ASCII 名再 scp。
> - clawbox-ap 已 enable:首次开机自动开热点引导配置;走完 /setup 向导后开机自动连已保存 WiFi 跳过热点。

---

## 部署后验证

```bash
systemctl is-active clawbox-setup      # 期望: active
systemctl is-active clawbox-gateway    # 期望: active
pgrep -af peripheral_daemon.py         # 期望: 有进程
curl -sI http://127.0.0.1/             # 期望: HTTP/1.1 302 Found
ss -tln | grep 18789                   # 期望: 网关监听中
```

2026-08-28 删板验证实测输出:

```
HTTP(:80):    HTTP/1.1 302 Found
网关(:18789): 监听中
Setup(:80):   active
守护进程:     运行中
```

---

## 常见问题

| 现象 | 处理 |
|---|---|
| `set: pipefail` 报错 / 脚本用 dash 跑 | 部署包 .sh 是 CRLF 行尾导致 shebang 失效;先转 LF: `sed -i 's/\r$//' deploy_all.sh` 及所有 .sh(2026-08-28 已修复包内文件, 重新传输即可) |
| gpiod Errno 2 | 已是修复版 install.sh(apt 装 python3-libgpiod);若旧包: `pip3 uninstall gpiod; apt install python3-libgpiod` |
| build Module not found | 前端须为标准布局(tsconfig `@/*→./src/*`);一键脚本已处理 |
| service 226/NAMESPACE | install_services.sh 已自动建目录;手工: `mkdir -p .bun .npm-global .npm .openclaw` + chown |
| install.sh 装 watchdog 卡死 | 已是修复版(policy-rc.d + timeout);旧包会因核桃派 systemd 挂起 |
| 启用看门狗后板子被重启 | 探针契约**已实机验证**(2026-08-26, 4 场景全对 + 90s 观察无重启);启用前仍建议 `bash watchdog_probe.sh` 确认返回 0 再 `CLAWBOX_ENABLE_WATCHDOG=1` |
| Unknown model / 微信不回 | 检查 auth_profile_store 表有 key;重启网关 |
| 风扇只到 30% 才转 / "嗡~翁" | 已修复(PWM 强制 100kHz);新曲线 40°C→20%、60°C→100% |
| 一键部署 build 失败后重跑被跳过 | deploy_all.sh 用 `.next/BUILD_ID` 判断已构建;失败残留会自动重 build |
| 首次开机没热点 | 一键脚本已 enable clawbox-ap(开机自动判断: 未配置→开热点, 已配置→连 WiFi);手工: `systemctl enable clawbox-ap` |
| 开机热点慢(1min+) | 已修复: start-ap.sh 的 `nmcli --wait 20` + disable NetworkManager-wait-online(一键脚本已含) |
| 前端 build 很慢(10-15 分钟) | 正常: 核桃派 T527 构建 Next 16 需较久;swap 已配 1.5G 防 OOM, 耐心等待即可 |

---

## 不要上传的内容

`node_modules/`、`.next/`、`__pycache__/`、`.pyc` —— 设备端会重新安装和构建。
