# 02 错误与根因分析 —— 2026-08-10/08-24/08-25/08-26/08-27 新设备部署

> 5 个错误,每个给出:现象 / 直接原因 / 根本原因 / 文件级修复 / 预防。
> 共同点:多数是"模板/脚本/代码隐含了对旧设备环境的假设",且"知识未固化成工具"。

---

## 错误 1:gpiod 全报 `Errno 2`(屏幕/LED/按键初始化失败)

- **现象**:新设备装完外设守护,屏幕/LED/按键全报 `[Errno 2] No such file or directory`,但 `/dev/gpiochip1` 存在。
- **直接原因**:`install.sh` 的 `import gpiod` 失败 → fallback `pip3 install gpiod` → 装 gpiod **2.5.0**(v2),链接 `libgpiod.so.3`;Debian 12 只有 libgpiod **v1.6.3**(`so.2`)→ 运行时加载共享库失败。
- **根本原因**:**包名假设错误 + 未验证干净环境**。Debian12 的 Python 绑定包叫 `python3-libgpiod`(不是 `python3-gpiod`)。install.sh 在"已手动装好依赖的原设备"开发,pip fallback 分支**从未执行过**,新设备首次触发。
- **文件级修复**:`epd-driver/install.sh`、`clawbox-peripheral/install.sh` 改 `apt install python3-libgpiod`(commit 06b6d0b)。
- **预防**:安装脚本里"从未执行过的分支"是最易藏 bug 的地方;新环境路径必须真机验证。

## 错误 2:前端 `build` Module not found

- **现象**:解压后 `app/components/lib` 散在项目根,tsconfig 已改 `@/*→./src/*`,build 找不到 `@/components/...`。
- **直接原因**:源码包是**扁平结构**(app 在根),设备端标准是 **src 目录结构**(app 在 `src/`)。
- **根本原因**:**知识没固化成工具**。8.4 日志已记录"部署结构 src 约定"的坑,但没变成脚本,每次靠人肉拆分 → 这次漏了。
- **文件级修复**:新增 `frontend-systemd/prepare_frontend.sh` 一键整理(配置留 根、源码进 src/、tsconfig 改; 该目录在部署包根目录, 部署时 scp 到设备 `/home/clawbox/clawbox-deploy/frontend-systemd/`)。本包 `前端源码/` 已整理好。
- **2026-08-25 演进**:前端源码已直接改为**设备端标准布局**(= 工作区 frontend 内容),`prepare_frontend.sh` 已从部署包移除, 无需再整理。
- **预防**:已知坑必须固化成幂等脚本,而不是只写日志。

## 错误 3:`clawbox-setup.service` 起不来(`226/NAMESPACE`)

- **现象**:服务 `activating (auto-restart)`,日志 `Failed to set up mount namespacing ... /home/clawbox/.bun: No such file or directory`。
- **直接原因**:`ProtectSystem=strict` 的 `ReadWritePaths` 列出 `.bun/.npm-global/.npm/.openclaw`,systemd 要求这些路径必须存在。
- **根本原因**:**模板隐含环境假设**。模板从旧设备导出,旧设备有这些历史目录,新设备是空系统。
- **文件级修复**:新增 `frontend-systemd/install_services.sh`,自动 `mkdir -p` + chown 这 4 个目录再装 service(同 prepare_frontend.sh, 位于部署包根目录, scp 到设备后执行)。
- **预防**:导出模板后要在干净环境验证;ReadWritePaths 的目录由安装脚本兜底创建。

## 错误 4:配了 Key 微信仍回 `Unknown model: deepseek-v4-flash`

- **现象**:网页配好 DeepSeek Key,微信发消息回 `model unavailable / Unknown model`,但 `curl DeepSeek /models` 有 `deepseek-v4-flash`。
- **直接原因**:前端 `configure/route.ts` 写 SQLite 用 `INSERT INTO auth_profile_store`,新设备 SQLite **没有该表** → 写入静默失败 → key 只进 `auth-profiles.json`(旧格式,**OpenClaw 运行时不读**)→ 网关无凭据 → 模型解析异常。
- **根本原因**:**前端对"表已存在"的脆弱假设 + 失败被吞**。旧设备表是 openclaw 早期建的;新 openclaw 2026.7.1-2 不建它。前端不建表、写失败也不报错给用户 → "看似成功实则没存"。
- **文件级修复**:改 `configure/route.ts` —— python 脚本加 `CREATE TABLE IF NOT EXISTS auth_profile_store`;写失败 `return 500` 报错给用户(不再静默)。
- **预防**:写关键存储必须幂等建表 + 失败要能反馈到用户。

## 错误 5:部署模板 tar 解压后文件散在根目录

- **现象**:解压 `frontend-systemd.tar.gz` 后 `cp frontend-systemd/...` 找不到,文件散在 `/home/clawbox/` 根。
- **直接原因**:打包用 `tar -czf x.tar.gz file1 file2`(无目录前缀),解压到当前目录,与"期望解到子目录"不一致。
- **根本原因**:**打包/解压路径契约未对齐**,属操作疏漏;本质仍是部署步骤未脚本化。
- **文件级修复**:本包以**目录结构**直接交付(scp 目录而非 tar),配合 `install_services.sh`(仍用),不再需要手工解压搬运; `prepare_frontend.sh` 已随前端源码改为标准布局而移除(2026-08-25)。
- **预防**:交付物用目录而非无结构 tar;打包统一带 `-C` 指定根。

---

## 错误 6:热点下网页连 WiFi 报 `Insufficient privileges`(热点不消失)

- **现象**:热点模式,网页配 WiFi 输密码后连不上,热点保持;前端日志 `Not authorized to control networking` / `Insufficient privileges`。
- **直接原因**:前端进程以 **clawbox 用户**调 `nmcli device wifi connect`,被 NetworkManager 的 **polkit 授权**拒绝(Debian 默认只放行 root/特定组)。板载按键能切热点,是因为走守护进程(root)。
- **根本原因**:**创建服务用户漏配附加组** —— 新设备 `useradd clawbox` 只建基础用户,没加 `netdev` 组(旧设备有);且 NM 1.42.4 对"新增并激活连接"授权较严,仅 netdev 组可能不够。
- **文件级修复**:`install_services.sh` 自动写 `/etc/polkit-1/rules.d/10-clawbox-network.rules` 显式放行 clawbox 控制 NetworkManager,并补附加组;装后重启 `clawbox-setup`。
- **预防**:创建服务用户必须核对附加组;涉及系统服务(NetworkManager)用 polkit 显式授权,不依赖默认组规则。

---

## 共性问题(6 个错误的共同根源)

1. **环境假设**:4/5 错误源于"模板/脚本/代码假设了旧设备环境状态"(包已装、目录已建、表已存在)——可移植性差。
2. **知识没变成工具**:8.4 的部署结构坑这次又踩,教训只躺在日志里,没固化成脚本。
3. **失败被静默吞掉**:错误 4 最典型——写库失败不报错,用户以为成功。
4. **干净环境从未验证**:install.sh fallback、service 模板都在成熟环境开发,空机首次暴露。

**对策(已在本包落地)**:
- 所有已知坑 → 幂等脚本(install.sh / prepare_frontend.sh / install_services.sh)
- 交付 = 目录结构 + 脚本,避免手工搬运
- 前端写关键存储:建表 + 报错
- 新部署务必先在空机跑一遍完整流程

---

## 2026-08-24 补充:新板部署看门狗 3 连坑(已在文件层根治)

### 错误 8:install.sh 装 watchdog 时卡死 6 分钟+
- **现象**:`apt-get install watchdog` 无限挂起,install.sh 卡在末尾(守护进程其实已先启动)。
- **根因**:watchdog 包 postinst 调 `deb-systemd-invoke start watchdog.service`,该 unit 是
  `Type=forking`,核桃派 systemd 对 start/restart 无限挂起(已知"systemd 卡住"坑,与 08-03 systemctl 卡死同源)。
- **修复**:装包前写 `/usr/sbin/policy-rc.d`(exit 101)阻止 postinst 启服务,装完即删 + `timeout 60` 兜底。

### 错误 9:启用看门狗后整板被重启(探针契约未验证)
- **现象**:nohup 启用 watchdog 后 ~1 分钟板子被物理重启(uptime 归零、`/tmp` 被清、前端源码丢失需重传)。
- **根因**:`watchdog_probe.sh`(watchdog.conf 的 test-binary)与守护进程心跳的契约未实机验证,
  喂狗失败 → 看门狗 60s 超时重启整板。
- **修复**:看门狗改 **opt-in**(`CLAWBOX_ENABLE_WATCHDOG=1` 才启用,默认不启用与旧板一致);
  启用走 nohup+rc.local(不走 systemd);启用前须手动验证 `timeout 5 watchdog -f` 与 probe 返回 0。

> **2026-08-26 实机验证(225, 探针契约已闭环)**:
> - probe 契约 4 场景全对: 健康=0 / 心跳过期=2 / 心跳缺失超宽限=1 / 进程死亡=3。
> - `CLAWBOX_ENABLE_WATCHDOG=1` install.sh 启用 → watchdog 进程 + watchdog.conf(60s/15s/probe) 就位;
>   90s 观察窗口(覆盖 60s 超时)uptime 持续增长, **板子未被重启**; 停用后进程/rc.local/systemd 全清。
> - 结论: 看门狗 opt-in 路径**已验证可用**, 启用前仍建议先跑 `bash watchdog_probe.sh` 确认返回 0。

### 错误 10:apt 装 watchdog 留下 enabled 的 systemd unit
- **现象/根因**:apt 装 watchdog 的 postinst update-rc.d fallback 建
  `default.target.wants/watchdog.service`,开机 systemd 会尝试启动而挂起/重启。
- **修复**:install.sh 装包后补 `systemctl disable watchdog 2>/dev/null || true`。

> 处理过程实录:杀掉挂起的 systemctl/dpkg/apt 进程链 → policy-rc.d 修 dpkg 半配置 →
> 验证 watchdog 守护进程本身正常(WD_ALIVE) → 改 install.sh → 重跑干净完成 →
> 现场 `systemctl disable watchdog` 清 enabled 符号链接。
